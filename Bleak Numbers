public:

	int is_bleak(int n)
	{
	    // Code here.
	   for(int i=1; i<=31;i++)
	    {
	        int num = n-i , o =0 ;
	        if(num>=0)
	        for(int j= 0; j<31;j++ )
	        {
	            if(num&(1<<j))
	            o++;
	        }
	        if(o==i)return 0 ;
	    }
	 return 1 ;
	}
